{% extends 'base.html.twig' %}

{% block body %}
    <h1>Мои объявления</h1>

    <table>
        <thead>
            <tr>
                <th>Дата</th>
                <th>Дисциплина</th>
                <th>Цена</th>
                <th>Валюта</th>
                <th>Длительность</th>
                <th>Время</th>
                <th>Сервис</th>
                <th>Дата окончания</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
        {% for ad in ads %}
            <tr>
                <td><a href="{{ path('ad_show', { 'idAd': ad.idAd }) }}">{% if ad.adate %}{{ ad.adate|date('Y-m-d') }}{% endif %}</a></td>
                <td>{{ ad.adText }}</td>
                <td>{{ ad.price }}</td>
                <td>{{ ad.currency }}</td>
                <td>{{ ad.durability }}</td>
                <td>{{ ad.value }}</td>
                <td>{{ ad.services }}</td>
                <td>{% if ad.dateServ %}{{ ad.dateServ|date('Y-m-d') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('ad_show', { 'idAd': ad.idAd }) }}">Показать</a>
                        </li>
                        <li>
                            <a href="{{ path('ad_edit', { 'idAd': ad.idAd }) }}">Редактировать</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ad_new') }}">Создать новое объявление</a>
        </li>
    </ul>
{% endblock %}
