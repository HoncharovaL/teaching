{% extends 'base.html.twig' %}

{% block body %}
    <h1>Adquery</h1>

    <table>
        <tbody>
            <tr>
                <th>Пользователь</th>
                <td>{{ adQuery.user }}</td>
            </tr>
            <tr>
                <th>Запрос</th>
                <td>{{ adQuery.queryText }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('adquery_index') }}">Назад</a>
        </li>
        <li>
            {{ form_start(confirm_form) }}
                <input type="submit" value="Подтвердить">
            {{ form_end(confirm_form) }}
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Удалить">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
